Sample of python conecting with mssql

in order to make the sample work please add an extra file call config.py with the following format:

    SERVER = 'Servername'
    USER = 'user'
    PASS = 'Password'
    DB = "DatabaseName"
